#!/usr/bin/env bash
#MISE description="Set up mooncakes.io credentials from environment"
set -euo pipefail

mkdir -p ~/.moon
cat > ~/.moon/credentials.json <<EOF
{
  "user_token": "${MOONCAKES_USER_TOKEN}"
}
EOF
echo "Credentials written to ~/.moon/credentials.json"
