#!/usr/bin/env bash
#MISE description="Type-check all example projects"
set -euo pipefail

for mod in examples/*/moon.mod.json; do
  dir=$(dirname "$mod")
  echo "==> Checking $dir"
  (cd "$dir" && moon check)
done
